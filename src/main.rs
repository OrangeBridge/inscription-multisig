use bdk::bitcoin::secp256k1::Secp256k1;

use bdk::bitcoin::{PrivateKey, PublicKey, Network};
use bdk::blockchain::rpc::Auth;
use bdk::keys::GeneratableDefaultOptions;
use bdk::keys::GeneratedKey;
use bdk::miniscript;
use inscription_multisig::ord_client::{server_online, OrdClient};
use std::fs::File;
use std::io::Write;
use std::path::Path;

#[tokio::main]
async fn main(){
}
fn get_multi_sig(){

}
